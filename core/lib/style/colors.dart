import 'package:flutter/material.dart';

class KColors {
  static Color kPrimaryColor = Colors.red[800] ?? Colors.red;
  static const Color kBackground = Colors.white;
  static const Color kTitleColor = Colors.grey;
  static const Color kTextColor = Colors.black;

  static var positiveColor = Colors.green[700] ?? Colors.green;
  static var negativeColor = Colors.redAccent[700] ?? Colors.red;
}
